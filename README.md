# Python Wrapper for Windows netsh Command for RSSI Scan

This is a simple wrapper to extract RSSI data from netsh output.

## Getting Started

Basic usage:

```
python wifi_rssi.py >> wifi_rssi_log.csv
```

## Note

Data will be send to output in CSV format.
